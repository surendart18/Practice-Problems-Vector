To Check the Given Position Bit of a Given Number is Set or Clear using Bitwise Operator


#include<stdio.h>
int main()
{
        int num,pos;
        printf("Enter the number & position: \n");
        scanf("%d%d",&num,&pos);
        (num>>pos)&1?puts("Set"):puts("Clear");
        //num&(1<<pos)?puts("Set"):puts("Clear");
}
