Delete All the nodes of List


void delete_all(ST **ptr)                // ST **ptr=&hptr;
{
        ST *temp;
        while(*ptr)
        {
                temp=*ptr;
                *ptr=temp->next;
                free(temp);
        }
}
