Delete All the nodes of List


void delete_all(ST **ptr)
{
        ST *temp;
        while(*ptr)
        {
                temp=*ptr;
                *ptr=temp->next;
                free(temp);
        }
}
