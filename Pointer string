Pssing the string to a function (passing pointer to the string)


#include<stdio.h>
void fun(char *);
int main()
{
        char a[20]="Vector India";
        fun(a);
}
void fun(char *p)
{
        printf("%s\n",p);
        for(int i=0;p[i];i++)
                printf("%c ",p[i]);
        printf("\n");
}
