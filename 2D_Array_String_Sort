Program to Read 10 Strings and Sort it in the Dictionary format (Lexicographical)
(Hint: Use the Bubble Sort And Strcmp function)


#include<stdio.h>
#include<string.h>
int main()
{
        char s[10][20],temp[20];
        printf("Enter the strings:\n");
        for(int i=0;i<10;i++)
                scanf(" %[^\n]",s[i]);
        for(int i=0;i<10;i++)
        {
                for(int j=i+1;j<10;j++)
                {
                        if(strcmp(s[i],s[j])>0)
                        {
                                strcpy(temp,s[i]);
                                strcpy(s[i],s[j]);
                                strcpy(s[j],temp);
                        }
                }
        }
        printf("Sorted Strings in Dictionary format :\n");
        for(int i=0;i<10;i++)
                printf("%s\n",s[i]);
}
