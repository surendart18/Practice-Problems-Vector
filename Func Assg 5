Program to Pass a value to a Function to display Fibonacci series upto given number without Return type function.


#include<stdio.h>
void fibo(int);
int main()
{
        int n,res;
        printf("Enter the N value:\n");
        scanf("%d",&n);
        fibo(n);
}
void fibo(int n)
{
        int z;
        for(int x=0,y=1;x<n;x=y,y=z)
        {
                printf("%d ",x);
                z=x+y;
        }
        printf("\n");
}
