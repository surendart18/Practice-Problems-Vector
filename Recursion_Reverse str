Program to Reverse the Given String using Recursion


#include<stdio.h>
void rev_str(char *,int,int);
int main()
{
        char s[50];
        int j;
        printf("Enter the string:\n");
        scanf(" %[^\n]",s);
        for(j=0;s[j];j++);
        rev_str(s,0,j-1);
        printf("%s\n",s);
}
void rev_str(char *s,int i,int j)
{
        char temp;
        if(i<j)
        {
                temp=s[i];
                s[i]=s[j];
                s[j]=temp;
                rev_str(s,i+1,j-1);
        }
}
