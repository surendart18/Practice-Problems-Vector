Write a Program to Read the Characters from Keyboard and Writes into a File


#include<stdio.h>
int main()
{
        char ch;
        FILE *fp=fopen("sample1.txt","w");

        printf("Enter the data into a file\n");
        while((ch=getchar())!=EOF)
                fputc(ch,fp);

        fclose(fp);
        printf("Data is successfully saved into a file\n");

        fp=fopen("sample1.txt","r");
        while((ch=fgetc(fp))!=EOF)
                printf("%c",ch);
}
