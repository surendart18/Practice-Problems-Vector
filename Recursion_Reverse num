Program to Reverse the Given Number Using Recursion Function


#include<stdio.h>
int rev_num(int,int);
int main()
{
        int n;
        printf("Enter the Number:\n");
        scanf("%d",&n);
        int rev=rev_num(n,0);
        printf("Reversed Number:%d\n",rev);
}
int rev_num(int num,int rev)
{
        int rem;
        if(num!=0)
                return rev_num(num/10,(rev*10)+(num%10));
        return rev;
}
