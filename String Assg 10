Program to convert all alphabets into its opposite case in a given string.



#include<stdio.h>
int main()
{
        char s[100];
        printf("Enter the character:\n");
        scanf(" %[^\n]",s);
        printf("%s\n",s);
        int i;
        for(i=0;s[i];i++)
        {
                if((s[i]>='A'&& s[i]<='Z')||(s[i]>='a' && s[i]<='z'))
                        s[i]^=32;
        }
        printf("Resultant string:\n");
        printf("%s\n",s);
}
