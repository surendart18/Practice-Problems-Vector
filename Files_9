Write a Program to Read the File Data more than once.


#include<stdio.h>
int main(int argc, char *argv[])
{
        if(argc!=2)
        {
                printf("Usage : ./a.out filename\n");
                return 0;
        }

        FILE *fp=fopen(argv[1],"r");

        if(fp==NULL)
        {
                printf("File doesn't exist\n");
                return 0;
        }

        char ch;
        while((ch=fgetc(fp))!=EOF)
                printf("%c",ch);

        rewind(fp);

        while((ch=fgetc(fp))!=EOF)
                printf("%c",ch);
        fclose(fp);
}
