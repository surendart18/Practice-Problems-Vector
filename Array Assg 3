Program to enter 10 elements into an array and find the particular given element is present or not.
If present display that how many no.of times the  element is present.



#include<stdio.h>
int main()
{
        int n,arr[10];
        for(int i=0;i<10;i++)
                scanf("%d",&arr[i]);
        printf("Enter the given number: \n");
        scanf("%d",&n);
        int count=0;
        for(int i=0;i<10;i++)
        {
                if(arr[i]==n)
                        count++;
        }
        printf("No.of times %d present in the array: %d\n",n,count);
}
