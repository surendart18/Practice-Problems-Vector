Program to Remove spaces in the given string line 


#include<stdio.h>
int main()
{
        char s[100];
        printf("Enter the string:\n");
        scanf(" %[^\n]",s);
        int i,j=0;
        for(i=0;s[i];i++)
        {
                if(s[i]!=' ')
                        s[j++]=s[i];
        }
        s[j]='\0';
        printf("%s\n",s);
}
