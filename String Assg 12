Program to copy  N no.of characters from the source array to destination array


#include<stdio.h>
int main()
{
        char s[20],d[20];
        int i,n;
        printf("Enter the string:\n");
        scanf(" %[^\n]",s);
        printf("Enter the N value:\n");
        scanf("%d",&n);
        for(i=0;i<n && s[i]!='\0';i++)
                d[i]=s[i];
        d[i]='\0';
        printf("%s\n",d);
}
