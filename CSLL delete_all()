Delete All the Nodes for Circular Linked List using the delete_all()


void delete_all(ST **ptr)            // ST **ptr=&hptr;
{
        if(*ptr==NULL)
                return;
        ST *temp=*ptr;
        ST *last;
        do
        {
                last=temp->next;
                free(temp);
                temp=last;
        }while(temp!=*ptr);

        *ptr=NULL;
}
