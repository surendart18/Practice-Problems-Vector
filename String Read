Program to read a string and display the string char by char (without space and newline and entire string line fixes)




#include<stdio.h>
int main()
{
        char s[20];
        printf("Enter the string:\n");
        scanf("%s",s);
        printf("%s\n",s);
        for(int i=0;s[i];i++)
                printf("%c",s[i]);
        printf("\n");
}
