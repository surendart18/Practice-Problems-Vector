To check given number is power of 2 or not using Bitwise Operator 


#include<stdio.h>
int main()
{
        int num;
        printf("Enter the number : \n");
        scanf("%d",&num);
        if((num>0) && ((num&(num-1))==0))
                printf("%d is Power of 2.\n",num);
        else
                printf("%d is not Power of 2.\n",num);
}
